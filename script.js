document.querySelector('.hamb')?.addEventListener('click',()=>document.querySelector('.links').classList.toggle('open'));const y=document.getElementById('year');if(y)y.textContent=new Date().getFullYear();async function loadHotDocAvailability(){const nodes=document.querySelectorAll('[data-hotdoc-doctor]');if(!nodes.length)return;try{const res=await fetch('assets/hotdoc-availability.json?ts='+Date.now(),{cache:'no-store'});if(!res.ok)throw new Error('Availability JSON not found');const data=await res.json();nodes.forEach(node=>{const name=node.getAttribute('data-hotdoc-doctor');const row=data.doctors&&data.doctors[name];node.textContent=row&&row.next?row.next:'Check HotDoc';const link=node.closest('.card')?.querySelector('[data-hotdoc-book]');if(link&&row&&row.bookingUrl)link.href=row.bookingUrl});document.querySelectorAll('[data-hotdoc-updated]').forEach(el=>{el.textContent=data.lastUpdated?'HotDoc availability last refreshed: '+data.lastUpdated:''})}catch(err){nodes.forEach(node=>node.textContent='Open HotDoc for live time');document.querySelectorAll('[data-hotdoc-updated]').forEach(el=>el.textContent='Live availability could not be loaded. Use HotDoc for the latest time.')}}loadHotDocAvailability();